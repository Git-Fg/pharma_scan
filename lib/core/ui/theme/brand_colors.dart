import 'package:flutter/material.dart';

/// Brand color constants for PharmaScan
/// These define the app's primary brand identity colors
abstract final class BrandColors {
  /// Primary brand color for light theme
  static const primaryLight = Color(0xFF0F766E);

  /// Primary brand color for dark theme
  static const primaryDark = Color(0xFF14B8A6);
}
