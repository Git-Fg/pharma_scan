{
  "name": "backend_pipeline",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build:db": "bun run src/index.ts && sqlite3 data/reference.db \".schema\" > data/schema.sql",
    "tool": "bun run tool/export_preview.ts && bun run tool/audit_data.ts",
    "preflight": "bun test && bun run build:db && bun run tool",
    "preflight:bp": "bun run build:db && bun run tool",
    "sync": "bun run tool/sync.ts",
    "update-db": "bun run tool/release.ts",
    "update-db-force": "bun run tool/release.ts --force"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@urbanzoo/remove-accents": "^1.0.2",
    "bun-sqlite-generate": "^0.0.2",
    "csv-parse": "^6.1.0",
    "iconv-lite": "^0.7.0",
    "zod": "^4.1.13"
  }
}