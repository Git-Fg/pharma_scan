{
  "name": "backend_pipeline",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "scripts": {
    "build:db": "bun run src/index.ts",
    "test": "bun test",
    "tool": "bun run tool/export_preview.ts && bun run tool/audit_unified.ts && bun run tool/verify_clusters.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@urbanzoo/remove-accents": "^1.0.2",
    "csv-parse": "^6.1.0",
    "iconv-lite": "^0.7.0",
    "zod": "^4.1.13"
  }
}
