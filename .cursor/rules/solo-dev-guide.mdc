---
alwaysApply: true
---
# Solo Developer Guide

Simple rules for building and maintaining PharmaScan. No corporate overhead—just practical patterns that work.

## Data Layer & Architecture

### Drift is the Domain Model

Drift-generated classes (`MedicamentRow`, `SpecialiteRow`) ARE your domain models.

* ✅ **Use directly:** Import `lib/core/database/database.dart` in UI.
* ❌ **Don't create:** "View Models" that just regroup data (e.g., `GroupedProductViewModel`).
* ✅ **Do create:** **SQL Views** (`views.drift`) that project exactly the columns and structure the UI needs. Let SQLite do the work.
* ✅ **Internal DTOs:** If you need to pass data between internal services, use **Dart Records** `({String a, int b})` instead of creating classes.

### SQL-First Logic

* **Aggregation:** Use `json_group_array()` and `GROUP BY` in Drift files to aggregate data. Do not fetch all rows and loop in Dart.
* **Search:** Use FTS5 tables joined with Summary tables. Use `bm25()` for ranking in SQL.

### Reactive Streams (Default)

* **Reads:** All UI data providers must return `Stream<T>` using `dao.watch...()`.
* **Why:** Ensures the UI is always consistent with the DB (e.g., after a background sync) without manual invalidation.
* **Writes:** Simple `Future<void>`.

### Database Safety

* Always use `watchSingleOrNull()` instead of `watchSingle()` to avoid crashes on concurrent deletions
* Use Drift's Expression API for dynamic queries—never string interpolation
* Since Drift 2.24.0+, `.isIn()` handles large sets automatically—no manual chunking needed

```dart
// ✅ CORRECT: Direct usage, Drift handles large sets
final query = db.select(db.table)..where((t) => t.id.isIn(ids));
final rows = await query.get();

// ❌ FORBIDDEN: Manual chunking boilerplate
for (var i = 0; i < ids.length; i += 900) { /* ... */ }
```

### Drift Managers Mandate

* **Rule:** For bulk data movements, push the work into SQL (views, inserts, helpers) and let Drift generate the APIs. Dart loops with per-row `customStatement()` calls are forbidden.
* **Rationale:** SQL-side operations are faster, deterministic, and benefit from SQLite's optimizer.
* **Reference Implementation:** `lib/core/database/queries.drift` owns the `search_index` virtual table plus the `insertSearchIndexFromSummary` helper. `DatabaseDao.populateFts5Index()` now just calls those generated statements.

```dart
// ✅ CORRECT: Let SQL do the heavy work
await database.deleteSearchIndex();
await database.insertSearchIndexFromSummary();

// ❌ FORBIDDEN: Loop and insert manually from Dart
for (final row in rows) {
  await customStatement('INSERT INTO search_index ...');
}
```

### FTS5 Native Tokenization

* **Rule:** Define FTS5 tables in `.drift` files (`CREATE VIRTUAL TABLE ... USING fts5(tokenize='trigram')`) so drift tracks them and migrations stay declarative.
* **Normalization:** Use the SQLite `normalize_text` function (registered in `configureAppSQLite()` via the `diacritic` package) to lowercase + strip diacritics inside SQL. Dart should never mutate strings before hitting the index.
* **Queries:** Always normalize user queries with the same logic (see `SearchDao`) to keep MATCH semantics consistent across languages.

### Group-First Architecture

* **Grouped medications:** Use `generique_groups.libelle` directly from `CIS_GENER` relationships
* **Standalone medications:** Use subtraction (rawName - officialForm - officialLab)
* **Dosage source:** Always from `principes_actifs` table, never from parsing names

### Trust the Relation, Not the String

* ❌ **Forbidden:** Writing Regex to clean chemical names (removing salts, hydrates, etc.).
* ✅ **Required:** Use the `CIS_COMPO` relationship (SA -> FT) to get the clean name provided by the database creators.
* **Why:** The database structure contains the truth; parsing is a last resort.

### Railway Oriented Programming

* **Rule:** For ETL pipelines, data transformations, and error-prone operations, use fpdart's `Either<Failure, Success>` or `TaskEither` for Railway Oriented Programming.
* **Rationale:** Functional error handling provides type-safe error propagation, composable transformations, and eliminates exception-based control flow.
* **Pattern:** Transform operations return `Either<ParseError, ParsedData>` instead of throwing exceptions. Chain operations with `flatMap`/`bind`.

```dart
// ✅ CORRECT: Functional error handling
Either<ParseError, ParsedData> parseFile(String content) {
  return parseHeader(content)
      .flatMap((header) => parseBody(header, content))
      .flatMap((body) => validateData(body));
}

// ❌ FORBIDDEN: Exception-based control flow
ParsedData parseFile(String content) {
  final header = parseHeader(content); // throws ParseError
  final body = parseBody(header, content); // throws ParseError
  return validateData(body); // throws ValidationError
}
```

### Source of Truth

* `MedicamentSummary` table is pre-aggregated—all Explorer/Search queries read from here
* All settings/preferences go in Drift `AppSettings` table—no `SharedPreferences`

### BDPM Data Ingestion Constraints (CRITICAL)

The BDPM (Public Meds Database) servers are legacy systems. They do not respect modern HTTP standards.

1. **No Header Trust:** You must **NEVER** rely on HTTP headers (`ETag`, `Last-Modified`, `Content-Length`) to determine if a file has changed. These headers are unreliable, absent, or stale on the official mirror.

2. **No HEAD Requests:** Do not attempt `HEAD` requests to check for updates. They will yield false negatives.

3. **Download & Hash:** The ONLY valid way to check for updates is to:

   * Download the full file (streaming).

   * Compute the SHA-256 hash locally.

   * Compare with the stored hash in `AppSettings`.

4. **No ETA Logic:** Do not attempt to calculate or display "Time Remaining" based on `Content-Length` header, as it is often missing (chunked transfer). Use indeterminate progress indicators or simple "downloaded bytes" counters if size is unknown.

## State Management (Riverpod)

### Provider Pattern

* **All providers use `@riverpod` code generation.**
* **Streams:** Prefer `Stream<T>` for any data coming from the DB.
* **Debounce:** For search, use `Future.delayed` inside the provider if strictly necessary, but prefer relying on the Stream's natural flow if the DB query is fast.
* **Synchronous state:** `Notifier<T>`
* **Asynchronous state:** `AsyncNotifier<T>`
* **Forbidden:** `StateProvider` (deprecated)

### AsyncValue Handling

When a provider returns `Future<T>`, `ref.watch(provider)` returns `AsyncValue<T>`, NOT `T`.

```dart
// ✅ CORRECT: Handle AsyncValue
final dataAsync = ref.watch(myProvider);
return dataAsync.when(
  data: (data) => ListView(...),
  loading: () => CircularProgressIndicator(),
  error: (err, stack) => ErrorWidget(err),
);

// ❌ FORBIDDEN: Direct access
final data = ref.watch(myProvider).value!; // Crashes if loading/error
```

### Pragmatism for Solo Dev

For simple cached config reads, a simple `Future<T>` with null check is fine. Don't over-engineer every operation.

```dart
// ✅ ACCEPTABLE: Simple Future for cached config
@riverpod
Future<String> theme(Ref ref) async {
  final settings = await ref.read(databaseServiceProvider).getSettings();
  return settings.theme;
}
```

### KeepAlive for Static Data

Use `@Riverpod(keepAlive: true)` for static config (dropdown lists, enums, settings) to prevent unnecessary re-queries.

## Accessibility

**CRITICAL:** All interactive widgets MUST have semantic labels for screen reader support.

* **FButton, FTile, FSelectTile:** Wrap with `Semantics` widget providing `label`, `hint`, and appropriate flags
* **Decorative Icons:** Wrap chevrons and decorative-only icons with `ExcludeSemantics`
* **All semantic labels MUST use `Strings.dart` constants** (project constraint)
* **Focus Management:** Use `FocusTraversalGroup` for logical keyboard navigation in forms

See `.cursor/rules/accessibility.mdc` for detailed patterns and examples.

## UI Patterns

### Two-Layer Separation

* **UI Layer:** Widgets, local state (`setState`), ConsumerWidgets
* **Service Layer:** Logic, DB access, parsing (zero `flutter/material.dart` dependencies)
* **Rule:** Business logic NEVER belongs in a Widget

### Boilerplate Reduction (Hooks)

* **Standard:** Use `HookConsumerWidget` for any widget needing a `Controller` (Text, Scroll, Animation) or Lifecycle listener.
* **FORBIDDEN:** `StatefulWidget` is explicitly prohibited. All widgets requiring controllers, lifecycle listeners, or local state MUST use `HookConsumerWidget`.
* **Exception:** None. If a widget needs state, it needs hooks.
* **Reference:** See `.cursor/rules/flutter-hooks.mdc` (if available) or standard hooks documentation.

### Mix UI Components & Theming

* **Theme Supremacy:** Use `context.theme.colors.*` and `context.theme.typography.*`.
* **Hardcoding Ban:** `Colors.red` or `TextStyle(fontSize: 14)` are strictly prohibited.
* **Use directly:** `FCard`, `FButton`, `FBadge`.
* **Styling:** Generate styles via CLI (`dart run forui style create...`) and access via theme extensions.

### Layout & Spacing

* **Protocol:** Follow `.cursor/rules/forui-sliver-guidelines.mdc`.
* **Root:** `FScaffold` > `CustomScrollView`.
* **Lists:** `SliverMainAxisGroup` > `SliverList`.
* **Use `gap` package** for spacing.

### Constraints Rule (Box Protocol)

"Constraints go down, Sizes go up. Parent sets position."

* Never place infinite widgets (`ListView`, unbounded `Column`) in unbounded parents
* Use `Expanded` or `Flexible` to break infinity
* Never use `IntrinsicHeight`/`IntrinsicWidth` in lists—performance killer

### Agnostic Components

Content widgets (forms, detail lists) must NEVER contain `Scaffold` or call `Navigator.pop()`. Expose callbacks (`onClose`, `onConfirm`) instead.

## Code Quality

### Widget Classes (Not Functions)

* ❌ **Forbidden:** Functions returning Widgets (`Widget _buildBadge()`)
* ✅ **Required:** Dedicated `StatelessWidget` classes or `HookConsumerWidget`.

### String Management

* **FORBIDDEN:** Hardcoded strings in `Text()` widgets
* **REQUIRED:** All user-facing strings in `lib/core/utils/strings.dart`

### Constants

* **Design tokens:** Use `AppDimens` and `context.theme`
* **Operational:** Use `AppConfig`
* **Local:** If specific to one widget, use `private static const` in that class

### Linting & Static Analysis

* **Linter:** `very_good_analysis` is enforced for strict type safety and modern Dart practices.
* **Documentation:** Javadoc-style comments for every public member are **not** required. The `public_member_api_docs` rule is disabled to reduce noise for solo developers.
* **Auto-fix:** Run `dart fix --apply` aggressively (up to 3 times) as some fixes expose new opportunities for other fixes.

## Testing

See `.cursor/rules/qa-testing.mdc` for testing patterns.
